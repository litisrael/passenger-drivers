// import express from "express";
// import { createServers } from "./createserver.js";

// export const initializeApp =async () => {
//   const routers = await createServers();
//   const app = express();

//   //midelwer
//   // app.use(morgan("dev"));
//   app.use(express.json());
 
//   // Routes
//   app.use("/passanger", //.postDriverManyDays);
//   app.use("/passanger", //.getDrivers );
//   app.use("/passanger", //.getDriver);
//   app.use("/passanger", //.deleteDriver);
//   app.use("/passanger", //.updatesDriver);
//   return app;
// };
